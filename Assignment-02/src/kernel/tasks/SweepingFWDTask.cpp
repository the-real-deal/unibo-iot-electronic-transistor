#include "SweepingFWDTask.hpp"
#include "Arduino.h"

SweepingFWDTask::~SweepingFWDTask()
{
}

void SweepingFWDTask::cleanup()
{
}

void SweepingFWDTask::excecute()
{
    // servo.on();
    // servo.setPosition(180);
    // for (int angle = SERVO_MIN_ANGLE; angle <= SERVO_MAX_ANGLE; angle += SERVO_STEP_ANGLE) {
    //     servo.setPosition(angle);
    //     delay(100);
    // }
    // servo.off();
    // setCompleted();
}