#include "SweepingBWDTask.hpp"
#include "Arduino.h"

SweepingBWDTask::~SweepingBWDTask()
{
}

void SweepingBWDTask::cleanup()
{
}

void SweepingBWDTask::excecute()
{
    // servo.on();
    // for(int angle = SERVO_MAX_ANGLE; angle >= SERVO_MIN_ANGLE; angle -= SERVO_STEP_ANGLE) {
    //     servo.setPosition(angle);
    //     delay(100);
    // }
    // servo.off();
    // setCompleted();
}